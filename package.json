{
  "name": "celo-hellocontracts",
  "version": "1.0.0",
  "description": "",
  "dependencies": {},
  "devDependencies": {
    "@typechain/truffle-v5": "^3.0.0",
    "truffle-typings": "^1.0.8",
    "typechain": "^4.0.0"
  },
  "scripts": {
    "compile": "truffle compile && typechain --target=truffle-v5 'build/contracts/*.json'",
    "migrate": "npm run compile && tsc -p ./tsconfig.migrate.json && truffle migrate --reset",
    "test": "tsc -p ./tsconfig.test.json && truffle test"
  },
  "author": "",
  "license": "ISC"
}
